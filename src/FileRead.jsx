import React from 'react'

import { Layout } from 'antd'

class FileRead extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <Layout className="file-read-page">File Read Page!!!</Layout>
        )
    }
}

export default FileRead;